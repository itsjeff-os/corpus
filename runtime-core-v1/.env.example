PORT=5061

# Optional. If set, all non-health endpoints require:
# Authorization: Bearer <token>
RUNTIME_CORE_TOKEN=
# Backward-compatible alias still accepted:
CODEX_RUNTIME_TOKEN=

# Supabase ACS substrate. RuntimeCore only needs server-side access.
RUNTIME_CORE_SUPABASE_URL=https://bxszabbhaidoxhzwyomt.supabase.co
RUNTIME_CORE_SUPABASE_SERVICE_ROLE_KEY=
RUNTIME_CORE_SUPABASE_FUNCTION_JWT=
RUNTIME_CORE_SUPABASE_PROJECT_REF=bxszabbhaidoxhzwyomt
RUNTIME_CORE_SUPABASE_USER_ID=
RUNTIME_CORE_SUPABASE_SCHEMA=acs

# 1Password SDK / Environments.
# OP_SERVICE_ACCOUNT_TOKEN is the only bootstrap secret that must exist on the mini-PC.
# Keep it out of Git and do not paste it into chat.
OP_ENVIRONMENT_ID=vsl3suvn4s2dnekt6hr5fpykl4
OP_SERVICE_ACCOUNT_TOKEN=

# Dedicated RuntimeCore Zep project key.
# Do not use Redo Ryan's Zep project key here.
# Prefer storing this in the 1Password Environment rather than plaintext here.
RUNTIME_CORE_ZEP_API_KEY=
RUNTIME_CORE_USER_ID=jeff
RUNTIME_CORE_THREAD_ID=runtime-core-v1

# Backward-compatible aliases still accepted:
CODEX_ZEP_API_KEY=
CODEX_USER_ID=
CODEX_THREAD_ID=
